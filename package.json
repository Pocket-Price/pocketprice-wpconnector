{
  "name": "pocketprice-connector",
  "version": "1.0.0",
  "description": "Pocket Price Connector — Gutenberg blocks for service prices",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
